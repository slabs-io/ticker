{
  "name" : "Ticker",
  "description" : "The heart of the trend engine, this module trigger a run of the network.",
  "type" : "ticker",
  "connectionsIn" : 0,
  "connectionsOut" : -1,
  "customIcon" : "fa-clock-o",
  "schema" : {
    "type": "object",
    "properties" : {
      "tickInterval": {
        "type": "integer",
        "default": 30,
        "minimum": 1,
        "maximum": 20000,
        "title": "Tick Interval (minutes)",
        "brief": "Interval"
      }
    }
  },
  "source": {
    "label": "Core Slab",
    "url": "http://slabs.io/"
  }
}
